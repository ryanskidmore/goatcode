import { ADVISOR_SCENARIOS } from "./advisor"
import { DEEP_WORKER_SCENARIOS } from "./deep-worker"
import { EXPLORER_SCENARIOS } from "./explorer"
import { ORCHESTRATOR_SCENARIOS } from "./orchestrator"
import { PLAN_BUILDER_SCENARIOS } from "./plan-builder"
import { RESEARCHER_SCENARIOS } from "./researcher"
import { WORKER_SCENARIOS } from "./worker"

export {
  ORCHESTRATOR_SCENARIOS,
  EXPLORER_SCENARIOS,
  ADVISOR_SCENARIOS,
  RESEARCHER_SCENARIOS,
  WORKER_SCENARIOS,
  DEEP_WORKER_SCENARIOS,
  PLAN_BUILDER_SCENARIOS,
}

export const ALL_EVAL_SCENARIOS = [
  ...ORCHESTRATOR_SCENARIOS,
  ...EXPLORER_SCENARIOS,
  ...ADVISOR_SCENARIOS,
  ...RESEARCHER_SCENARIOS,
  ...WORKER_SCENARIOS,
  ...DEEP_WORKER_SCENARIOS,
  ...PLAN_BUILDER_SCENARIOS,
]
