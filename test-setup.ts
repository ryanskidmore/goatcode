import { beforeEach } from "bun:test"

// Reset module-level state before each test to prevent test pollution.
// Add state reset functions here as new stateful modules are added.
beforeEach(() => {
  // Currently no stateful module state to reset.
  // Future stateful modules will register reset functions here.
})
